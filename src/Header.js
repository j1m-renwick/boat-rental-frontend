import React from "react";

export default function Header() {

    return (
        <div className="header">
            <h2>Who to follow</h2><a href="#" id="refreshBtn" className="refresh">Refresh</a>
        </div>
    )
}